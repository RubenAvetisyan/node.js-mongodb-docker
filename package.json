{
  "name": "nodejs-mongodb-docker",
  "version": "1.0.0",
  "description": "simple project (only API without UI) on Stack: nodejs(express), mongodb, docker",
  "scripts": {
    "dev": "cross-env nodemon ./server.js --watch",
    "start": "cross-env NODE_ENV=production nodemon ./server.js --watch",
    "lint:js": "eslint --fix --ext .js --ignore-path .gitignore ."
  },
  "author": "RubenAvetisyan",
  "license": "ISC",
  "dependencies": {
    "bcrypt-nodejs": "0.0.3",
    "body-parser": "^1.19.0",
    "compression": "^1.7.4",
    "cross-env": "^7.0.2",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.10.13",
    "nodemon": "^2.0.6",
    "passport": "^0.4.1",
    "passport-jwt": "^4.0.0"
  },
  "devDependencies": {
    "eslint": "^7.13.0"
  }
}
